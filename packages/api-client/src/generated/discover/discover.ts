/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * LitMood API
 * 책·영화·음악을 무드와 함께 기록하고 나누는 서비스의 API
 * OpenAPI spec version: v1
 */
import { apiFetcher } from '../../fetcher'
import type { ContentRef, PopularParams } from '.././model'

/**
 * 기간 내 기록 수 기준. Redis Sorted Set 으로 집계한다.
 * @summary 인기 콘텐츠
 */
export type popularResponse200 = {
  data: ContentRef[]
  status: 200
}

export type popularResponseSuccess = popularResponse200 & {
  headers: Headers
}
export type popularResponse = popularResponseSuccess

export const getPopularUrl = (params?: PopularParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/discover/popular?${stringifiedParams}`
    : `/api/v1/discover/popular`
}

export const popular = async (
  params?: PopularParams,
  options?: RequestInit,
): Promise<popularResponse> => {
  return apiFetcher<popularResponse>(getPopularUrl(params), {
    ...options,
    method: 'GET',
  })
}
