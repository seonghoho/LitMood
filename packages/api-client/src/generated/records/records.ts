/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * LitMood API
 * 책·영화·음악을 무드와 함께 기록하고 나누는 서비스의 API
 * OpenAPI spec version: v1
 */
import { apiFetcher } from '../../fetcher'
import type {
  CreateRecordRequest,
  FeedParams,
  MyRecordsByContentParams,
  MyTimelineParams,
  RecordPage,
  RecordResponse,
  UpdateRecordRequest,
} from '.././model'

/**
 * 콘텐츠가 자체 DB 에 없으면 provider 에서 가져와 스냅샷을 만든다. 필수 입력은 status 뿐이다.
 * @summary 기록 생성
 */
export type createResponse200 = {
  data: RecordResponse
  status: 200
}

export type createResponseSuccess = createResponse200 & {
  headers: Headers
}
export type createResponse = createResponseSuccess

export const getCreateUrl = () => {
  return `/api/v1/records`
}

export const create = async (
  createRecordRequest: CreateRecordRequest,
  options?: RequestInit,
): Promise<createResponse> => {
  return apiFetcher<createResponse>(getCreateUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createRecordRequest),
  })
}

/**
 * 공개 범위를 벗어나면 404 로 응답한다
 * @summary 기록 단건 조회
 */
export type getResponse200 = {
  data: RecordResponse
  status: 200
}

export type getResponseSuccess = getResponse200 & {
  headers: Headers
}
export type getResponse = getResponseSuccess

export const getGetUrl = (id: number) => {
  return `/api/v1/records/${id}`
}

export const get = async (id: number, options?: RequestInit): Promise<getResponse> => {
  return apiFetcher<getResponse>(getGetUrl(id), {
    ...options,
    method: 'GET',
  })
}

/**
 * soft delete
 * @summary 기록 삭제
 */
export type _deleteResponse200 = {
  data: void
  status: 200
}

export type _deleteResponseSuccess = _deleteResponse200 & {
  headers: Headers
}
export type _deleteResponse = _deleteResponseSuccess

export const getDeleteUrl = (id: number) => {
  return `/api/v1/records/${id}`
}

export const _delete = async (id: number, options?: RequestInit): Promise<_deleteResponse> => {
  return apiFetcher<_deleteResponse>(getDeleteUrl(id), {
    ...options,
    method: 'DELETE',
  })
}

/**
 * @summary 기록 수정
 */
export type updateResponse200 = {
  data: RecordResponse
  status: 200
}

export type updateResponseSuccess = updateResponse200 & {
  headers: Headers
}
export type updateResponse = updateResponseSuccess

export const getUpdateUrl = (id: number) => {
  return `/api/v1/records/${id}`
}

export const update = async (
  id: number,
  updateRecordRequest: UpdateRecordRequest,
  options?: RequestInit,
): Promise<updateResponse> => {
  return apiFetcher<updateResponse>(getUpdateUrl(id), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(updateRecordRequest),
  })
}

/**
 * 커서 기반 페이지네이션
 * @summary 내 타임라인
 */
export type myTimelineResponse200 = {
  data: RecordPage
  status: 200
}

export type myTimelineResponseSuccess = myTimelineResponse200 & {
  headers: Headers
}
export type myTimelineResponse = myTimelineResponseSuccess

export const getMyTimelineUrl = (params?: MyTimelineParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types', 'status', 'moods']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/records/me?${stringifiedParams}`
    : `/api/v1/records/me`
}

export const myTimeline = async (
  params?: MyTimelineParams,
  options?: RequestInit,
): Promise<myTimelineResponse> => {
  return apiFetcher<myTimelineResponse>(getMyTimelineUrl(params), {
    ...options,
    method: 'GET',
  })
}

/**
 * 검색 결과 중 이미 기록한 것을 가려낸다 (F-03-01). refs 는 `PROVIDER:externalId` 형식으로 여러 번 준다.
기록이 없는 콘텐츠는 응답에서 빠지므로, 없다고 404 가 나지는 않는다.
검색 응답에 실을 수 없는 정보다 — 검색 결과는 캐시되므로 사용자별 값을 섞으면 다른 사람에게 샌다.

 * @summary 콘텐츠로 내 기록 찾기
 */
export type myRecordsByContentResponse200 = {
  data: RecordResponse[]
  status: 200
}

export type myRecordsByContentResponseSuccess = myRecordsByContentResponse200 & {
  headers: Headers
}
export type myRecordsByContentResponse = myRecordsByContentResponseSuccess

export const getMyRecordsByContentUrl = (params?: MyRecordsByContentParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['refs']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/records/me/by-content?${stringifiedParams}`
    : `/api/v1/records/me/by-content`
}

export const myRecordsByContent = async (
  params?: MyRecordsByContentParams,
  options?: RequestInit,
): Promise<myRecordsByContentResponse> => {
  return apiFetcher<myRecordsByContentResponse>(getMyRecordsByContentUrl(params), {
    ...options,
    method: 'GET',
  })
}

/**
 * 팔로우한 사용자들의 공개·팔로워 공개 기록. 차단한 사용자는 제외된다.
 * @summary 팔로잉 피드
 */
export type feedResponse200 = {
  data: RecordPage
  status: 200
}

export type feedResponseSuccess = feedResponse200 & {
  headers: Headers
}
export type feedResponse = feedResponseSuccess

export const getFeedUrl = (params?: FeedParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types', 'moods']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/records/feed?${stringifiedParams}`
    : `/api/v1/records/feed`
}

export const feed = async (params?: FeedParams, options?: RequestInit): Promise<feedResponse> => {
  return apiFetcher<feedResponse>(getFeedUrl(params), {
    ...options,
    method: 'GET',
  })
}
