/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * LitMood API
 * 책·영화·음악을 무드와 함께 기록하고 나누는 서비스의 API
 * OpenAPI spec version: v1
 */
import { apiFetcher } from '../../fetcher'
import type { SearchParams, SearchResponse } from '.././model'

/**
 * 책·영화·음악을 동시에 검색한다. 일부 provider 가 실패해도 나머지 결과를 반환한다.
 * @summary 통합 검색
 */
export type searchResponse200 = {
  data: SearchResponse
  status: 200
}

export type searchResponseSuccess = searchResponse200 & {
  headers: Headers
}
export type searchResponse = searchResponseSuccess

export const getSearchUrl = (params: SearchParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['types']

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : v.toString())
      })
      return
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/contents/search?${stringifiedParams}`
    : `/api/v1/contents/search`
}

export const search = async (
  params: SearchParams,
  options?: RequestInit,
): Promise<searchResponse> => {
  return apiFetcher<searchResponse>(getSearchUrl(params), {
    ...options,
    method: 'GET',
  })
}
