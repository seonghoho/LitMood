/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * LitMood API
 * 책·영화·음악을 무드와 함께 기록하고 나누는 서비스의 API
 * OpenAPI spec version: v1
 */
import { apiFetcher } from '../../fetcher'
import type {
  AdminReportPage,
  AdminReportResponse,
  ReportResolutionRequest,
  ReportsParams,
} from '.././model'

/**
 * PENDING 인 신고만 처리할 수 있다. 이미 처리된 건은 409 다.
 * @summary 신고 처리
 */
export type resolveResponse200 = {
  data: AdminReportResponse
  status: 200
}

export type resolveResponseSuccess = resolveResponse200 & {
  headers: Headers
}
export type resolveResponse = resolveResponseSuccess

export const getResolveUrl = (id: number) => {
  return `/api/v1/admin/reports/${id}`
}

export const resolve = async (
  id: number,
  reportResolutionRequest: ReportResolutionRequest,
  options?: RequestInit,
): Promise<resolveResponse> => {
  return apiFetcher<resolveResponse>(getResolveUrl(id), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(reportResolutionRequest),
  })
}

/**
 * 최근 접수 순으로 돌려준다. status 를 비우면 상태를 가리지 않는다. 각 항목에는 대상의 이름과, 같은 대상에 쌓인 신고 건수가 함께 실린다.
 * @summary 신고 큐
 */
export type reportsResponse200 = {
  data: AdminReportPage
  status: 200
}

export type reportsResponseSuccess = reportsResponse200 & {
  headers: Headers
}
export type reportsResponse = reportsResponseSuccess

export const getReportsUrl = (params?: ReportsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/admin/reports?${stringifiedParams}`
    : `/api/v1/admin/reports`
}

export const reports = async (
  params?: ReportsParams,
  options?: RequestInit,
): Promise<reportsResponse> => {
  return apiFetcher<reportsResponse>(getReportsUrl(params), {
    ...options,
    method: 'GET',
  })
}
