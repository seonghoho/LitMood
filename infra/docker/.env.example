# 로컬 포트 오버라이드 (선택)
# 다른 프로젝트가 표준 포트를 점유 중일 때 사용한다.
# 이 파일을 infra/docker/.env 로 복사한 뒤 값을 조정하고,
# apps/api/.env.local 의 DB_URL / REDIS_PORT 도 함께 맞춘다.
POSTGRES_PORT=5432
REDIS_PORT=6379
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
