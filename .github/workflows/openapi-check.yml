# ADR-008 — OpenAPI 를 API 계약의 단일 진실 원천으로 유지한다.
# 컨트롤러 변경이 스펙에 반영되지 않았거나, 의도치 않은 breaking change 가
# 포함된 PR 을 여기서 차단한다.
name: OpenAPI 계약 검사

on:
  pull_request:
    paths: ['apps/api/**', 'packages/api-client/**']

jobs:
  contract:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: litmood
          POSTGRES_USER: litmood
          POSTGRES_PASSWORD: litmood_local
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U litmood" --health-interval 5s
          --health-timeout 3s --health-retries 10
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping" --health-interval 5s
          --health-timeout 3s --health-retries 10

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '21', cache: gradle }

      # 빌드와 기동을 분리한다.
      # bootRun 은 컴파일까지 포함하므로, 콜드 러너에서는 Gradle 배포판 내려받기와
      # 컴파일에만 2분 이상 걸려 헬스체크 대기 창을 넘긴다. 먼저 jar 를 만들어 두면
      # 기동 자체는 몇 초라 대기가 안정적이다.
      - name: 빌드
        working-directory: apps/api
        run: ./gradlew bootJar --no-daemon

      - name: 애플리케이션 기동
        working-directory: apps/api
        env:
          DB_URL: jdbc:postgresql://localhost:5432/litmood
          DB_USERNAME: litmood
          DB_PASSWORD: litmood_local
          JWT_SECRET: ci-only-not-a-real-secret-value-padding-32b
        run: |
          java -jar build/libs/*.jar > /tmp/api.log 2>&1 &
          for i in $(seq 1 40); do
            curl -sf http://localhost:8080/actuator/health >/dev/null && break
            sleep 3
          done
          if ! curl -sf http://localhost:8080/actuator/health >/dev/null; then
            echo "::error::애플리케이션이 기동하지 않았습니다"
            tail -50 /tmp/api.log
            exit 1
          fi

      - name: 스펙 추출
        run: curl -sf http://localhost:8080/v3/api-docs.yaml -o /tmp/openapi.yaml

      - name: 커밋된 스펙과 비교
        run: |
          if [ ! -f packages/api-client/openapi.yaml ]; then
            echo "::warning::커밋된 스펙이 없습니다. 최초 생성 후 커밋하세요."
            exit 0
          fi
          if ! diff -u packages/api-client/openapi.yaml /tmp/openapi.yaml; then
            echo "::error::OpenAPI 스펙이 컨트롤러와 어긋납니다."
            echo "다음 명령으로 갱신 후 커밋하세요:"
            echo "  pnpm --filter @litmood/api-client fetch-spec"
            exit 1
          fi

      # 호환성 파괴 변경 탐지 — 프론트가 깨지기 전에 PR 단계에서 잡는다
      - name: Breaking change 검사
        uses: oasdiff/oasdiff-action/breaking@main
        continue-on-error: true
        with:
          base: 'https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/packages/api-client/openapi.yaml'
          revision: /tmp/openapi.yaml

  # 커밋된 생성 타입이 스펙과 어긋나지 않는지 본다.
  # 생성물을 커밋하기로 했으므로(백엔드를 띄우지 않고도 프론트가 빌드된다),
  # "커밋된 것 == 스펙에서 생성된 것" 을 지켜 주는 검사가 따로 필요하다.
  # 백엔드 기동은 필요 없다 — 커밋된 openapi.yaml 만 읽는다.
  codegen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @litmood/api-client codegen
      - name: 생성 결과가 커밋과 같은지 비교
        run: |
          if ! git diff --exit-code -- packages/api-client/src/generated; then
            echo "::error::생성 타입이 openapi.yaml 과 어긋납니다."
            echo "다음 명령으로 갱신 후 커밋하세요:"
            echo "  pnpm --filter @litmood/api-client codegen"
            exit 1
          fi
